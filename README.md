# Security-monitoring-system
A read-only security monitoring system that connects to a remote Linux host via SSH, collects logs and telemetry, stores them locally, and displays them in a real-time web dashboard.
